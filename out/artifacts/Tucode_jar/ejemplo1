numero definir factorial(numero x) {
	numero resultado = 1;
	mientras (x > 0) {
		resultado = resultado * x;
		x = x - 1;
	}
}

numero comienzo() {
	numero var = factorial(5);
	imprimir(var);
	devolver 0;
}