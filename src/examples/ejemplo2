&& Este es un comentario de una linea.
numero comienzo() {
	booleano x = (verdadero y falso) o (84 <= 0);
	si(!x) {
		numero y = 0;
		hacer {
			imprimirln(y);
			y = y+2;
		} mientras(y <= 20);
	}
	sino {
		repetir(10) {
			imprimirln("La expresion era verdadera");
		}
	}
	devolver 0;
}
