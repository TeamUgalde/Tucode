numero comienzo() {
	texto definir procesar(texto s) {
		mayuscula(variable) = simbolo caracter;
		mayuscula(variable);
		si (== 1 2) {
			texto resultado = mayuscula(s);
			numero var = 2 + 4 / 2
		}
		sino {
			caracter devolver
		}
	}
}
